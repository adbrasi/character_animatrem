{
  "display_name": "anima-character-outfit-described",
  "description": "Anima character + outfit, DESCRIBED mode (the correct Anima path): character as the subject, trigger_outfit included as a selection token, AND the clothing described normally (color/fabric/cut) in the anima-character hybrid tags+prose format. Recommended default. Accepts a custom instruction.",
  "variables": [
    {
      "name": "trigger_character",
      "prompt": "Character trigger name (e.g. aria) — placed at the START as the subject",
      "required": true,
      "default": ""
    },
    {
      "name": "trigger_outfit",
      "prompt": "Outfit trigger name (e.g. aria_beach) — included as a token; the outfit IS also described",
      "required": true,
      "default": ""
    },
    {
      "name": "custom_instruction",
      "prompt": "Optional operator instruction for the LLM on this outfit/dataset (leave empty to skip)",
      "required": false,
      "default": ""
    }
  ]
}
